type User {
  id: ID! @id
  name: String!
}

type Income {
  id: ID! @id
  bType: String!
  category: String!
  cost: Float!
}

type Expense {
  id: ID! @id
  bType: String!
  category: String!
  cost: Float!
}