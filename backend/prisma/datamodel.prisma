type User {
  id: ID! @id
  name: String!
}

type Income {
    bType: String!
    category: ID! @id 
    cost: Float!
  }
  
  type Expense {
    bType: String!
    category: ID! @id
    cost: Float!
  }